import React from 'react';
import { Document, Page, pdfjs } from "react-pdf";
interface Props {
    pdfadd: string;
    name: string;
    journelintro: string;
    journeldate: string;
    
}

function Modalbox({
    pdfadd,
    name,
    journelintro,
    journeldate,
    
}: Props) {
    return (
        <div>
            
        </div>
    );
}

export default Modalbox;